<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Test APK only. Reads only the caller-supplied bounded FD and
             returns its length/digest; has no file or network authority. -->
        <service
            android:name="dev.animetv.anime_tv.aniyomi.AniyomiHttpReplyTestService"
            android:exported="true"
            android:isolatedProcess="true"
            android:process=":http_body_fixture" />
    </application>
</manifest>
